# Production Environment Configuration Template for FitForge
# DO NOT commit actual credentials to repository!
# Set these in Digital Ocean App Platform environment variables

# Application
NODE_ENV=production
PORT=5000

# Supabase Configuration (Replace with actual values in DO App Platform)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-from-supabase-dashboard

# Note: Service role key should be configured in Digital Ocean dashboard as secret
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-dashboard

# SECURITY WARNING: 
# - This file contains template values only
# - Real credentials must be set via Digital Ocean App Platform environment variables
# - Never commit actual API keys, JWTs, or credentials to the repository